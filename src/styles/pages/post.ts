import { styled } from '..';

export const PostContainer = styled('main', {
  width: '100%',
  maxWidth: '54rem',
  display: 'flex',
  flexDirection: 'column',
});

export const PostReadmeContent = styled('div', {
  padding: '2rem 2.5rem',
});
